public class HalloweenTest {

    //actually this is just for test
    //we created a new project and created class both in main and test under java


    //right click the file-->git--> add to gitignore-----> this is also the way to put the file into git ignore
    //but before doing all these, we should create or initialize git/version control by enableing version control in VCS
    //this is use for one time for each project, later you don't see that function in VCS

}
//commit--> command + k
// push --> command + shift +k
//pull/update --> command+t